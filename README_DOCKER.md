# 🐝 La Abejita - Docker

## Requisitos
- Docker Desktop instalado
- Docker Compose instalado

## Configuración

1. Asegúrate de tener un archivo `.env` en la raíz con el codigo DATABASE_URL

2. Construye y levanta el contenedor:
   ```bash
   docker-compose up --build

   http://localhost:5000


---

## Paso 5: Prueba en tu máquina

```bash
# Construye la imagen
docker-compose up --build

🐝 Servidor corriendo en http://localhost:5000